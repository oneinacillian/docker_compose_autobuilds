apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hyperion
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/oneinacillian/docker_compose_autobuilds
    targetRevision: main
    path: k3s-deploy/hyperion-stack-deploy/hyperion-app
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: hyperion
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true 